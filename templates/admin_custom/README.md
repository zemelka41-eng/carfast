# Custom admin templates

Сторонние правки админки держим здесь, чтобы не перекрывать стандартные файлы Django (`templates/admin/...`). Сейчас кастомных шаблонов нет, используется дефолтный `admin/base.html`.

